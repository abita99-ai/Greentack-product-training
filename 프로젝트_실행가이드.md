# 🚀 프로젝트 실행 가이드 (Project Execution Guide)

본 프로젝트는 **Apple Design Style** 테마 및 **페이퍼로지(Paperlogy)** 서체가 적용된 미니멀 반응형 웹 서비스입니다. 
로컬 환경 및 웹 브라우저 로딩 시 이미지 깨짐이나 경로 오작동 등 **동일한 오류가 발생하지 않도록 방지하는 실행 방법**을 제시합니다.

---

## 📌 주요 사양 및 구현 특징

- **디자인 테마**: Apple Design Style (라이트 모드, Translucent 헤더, 둥근 카드 레이아웃)
- **폰트**: 페이퍼로지 (Paperlogy) 웹폰트
- **인터랙션**: 마우스 호버 시 2초 이내 작동하는 노란색 번개 스파크 및 빗방울 낙하 애니메이션
- **반응형 디자인**: 768px 이하 모바일 화면 시 세로 배치 헤더 및 손가락 터치에 최적화된 버튼 패딩
- **이미지 호환성**: 로컬 경로(`file://`) 보안 제약 및 404 상대경로 오작동을 영구적으로 방지하기 위해 **프로필 아바타 이미지를 Data URI(Base64) 형태**로 내장 처리하여 100% 무결성을 보장합니다.

---

## 💡 권장 실행 가이드 (이미지/경로 오류 방지)

폴더명에 **한글**이나 **공백**이 포함된 경로에서 `index.html` 파일을 브라우저로 직접 두 번 클릭하여 실행(`file:///` 프로토콜)할 경우, 브라우저 보안 정책에 따라 일부 외부 리소스가 차단되거나 브라우저 서브에이전트에서 404 렌더링 오류가 발생할 수 있습니다. 

따라서 아래의 **로컬 웹 서버 서빙 방식** 중 하나를 선택하여 실행할 것을 강력히 권장합니다.

---

### Method 1. VS Code `Live Server` 사용 (가장 추천 ⭐)

1. VS Code에서 본 프로젝트 폴더(`exercise antigravity`)를 엽니다.
2. 확장 탭(`Ctrl + Shift + X`)에서 **Live Server** (by Ritwick Dey)를 설치합니다.
3. `index.html` 파일을 마우스 우클릭한 후 **`Open with Live Server`** (`Alt + L, Alt + O`)를 클릭합니다.
4. 브라우저가 자동 구동되며 `http://127.0.0.1:5500/index.html` 주소로 접속되어 모든 기능이 깨짐 없이 원활하게 구동됩니다.

---

### Method 2. Node.js `npx serve` 이용

프로젝트 폴더 터미널에서 다음 명령어를 실행합니다:

```bash
npx serve
```

실행 후 터미널에 표시되는 URL (예: `http://localhost:3000`)을 브라우저 주소창에 입력하여 접속합니다.

---

### Method 3. Python 내장 HTTP 서버 이용

터미널에서 다음 명령어를 실행합니다:

```bash
# Python 3.x
python -m http.server 8080
```

실행 후 웹 브라우저에서 `http://localhost:8080` 주소로 접속합니다.

---

## 📱 모바일 화면 확인 방법

1. 위의 로컬 웹 서버 방식 중 하나로 접속한 상태에서 **`F12`** 키를 눌러 브라우저 개발자 도구를 엽니다.
2. **`Ctrl + Shift + M`** (또는 좌측 상단 스마트폰/아이콘)을 클릭하여 **모바일 뷰 모드**로 전환합니다.
3. 화면 너비를 `768px` 이하(예: `390px` iPhone / `412px` Galaxy)로 조절하면 세로형 모바일 네비게이션과 터치 최적화 버튼을 확인할 수 있습니다.

---

## 📁 프로젝트 파일 구조

```
exercise antigravity/
├── index.html               # 메인 HTML (Apple 스타일, CSS, JS, Data URI 포함)
├── 프로젝트_실행가이드.md       # 실행 방식 및 오류 방지 안내 문서 (본 문서)
├── README.md                # GitHub 저장소 설명 문서
└── images/                  # 아바타 및 원본 이미지 폴더
    ├── avatar1.jpg          # 김민준 개발자 프로필 원본
    └── avatar2.jpg          # 이지은 디자이너 프로필 원본
```

---

## 🔄 Git 원격 저장소 동기화

원격 저장소에 최신 변경 사항이 업로드되어 있으며, 코드 수정 후 재업로드 시 아래 명령어로 진행할 수 있습니다.

```bash
git add .
git commit -m "docs: Add project execution guide markdown file"
git push origin main
```
